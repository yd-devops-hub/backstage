import { createRouteRef } from '@backstage/frontend-plugin-api';

/** @public */
export const inboxRouteRef = createRouteRef();

/** @public */
export const mineRouteRef = createRouteRef();

/** @public */
export const detailRouteRef = createRouteRef();
